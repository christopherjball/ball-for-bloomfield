---
const DONATE_URL = 'https://donate.stripe.com/14A5kC5X02Sk1bu1Engbm00'; // TODO: set your donation link
const CONTACT_EMAIL = 'chris@ballforbloomfield.com'; // TODO: confirm email
---

<astro:head>
    <!-- ...existing meta... -->
    <meta property="og:title" content="Ball for Bloomfield — Chris Ball for Mayor" />
    <meta property="og:description" content="Practical leadership through strong partnerships. Let’s fix the basics and dream together." />
    <meta property="og:image" content="/images/logo.png" />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
</astro:head>

<!-- HERO -->
<header class="hero" role="banner">
    <div class="wrap">
        <img src="/images/logo.png" alt="Ball for Bloomfield Logo" class="hero-logo" />
        <h1 class="title">Bloomfield works best when we work together.</h1>
        <p class="tag">Practical leadership. Strong basics. Community pride.</p>
        <nav class="cta" aria-label="Primary">
            <a class="btn primary" href="#sign">Request a sign</a>
            <a class="btn" href={DONATE_URL}>Contribute</a>
        </nav>
    </div>
</header>

<main id="main" tabindex="-1">
    <!-- PRIORITIES -->
    <section id="priorities" class="section alt" aria-labelledby="priorities-heading">
        <div class="wrap">
            <h2 id="priorities-heading" class="section-title">Priorities</h2>
            <div class="cards">
                <article class="card">
                    <h3>Strong Infrastructure</h3>
                    <p>Modernize electric, water, and sewer using state & federal funding so we stop wasting money on outdated systems.</p>
                </article>
                <article class="card">
                    <h3>Downtown & Housing</h3>
                    <p>Keep the square vibrant and add homes for families through city–business–Main Street partnerships.</p>
                </article>
                <article class="card">
                    <h3>Efficiency & Resilience</h3>
                    <p>Cut waste, strengthen the grid, and prepare for storms – preventative maintenance for the whole town.</p>
                </article>
                <article class="card">
                    <h3>Dream Together & Involve Youth</h3>
                    <p>Invite the whole community – especially our youth – to help shape Bloomfield’s future and make good ideas real.</p>
                </article>
            </div>
        </div>
    </section>

    <!-- ABOUT -->
    <section id="about" class="section" aria-labelledby="about-heading">
        <div class="wrap narrow about-grid">
            <img src="/images/chris.png" alt="Chris Ball in Bloomfield" class="headshot" width="600" height="600" loading="lazy" decoding="async" />
            <div>
                <h2 id="about-heading" class="section-title">About Chris</h2>
                <p>
                    Bloomfield is my home of choice. I've lived in Davis County since 2000, raised my family here, and worked with our community to bring in
                    resources that make a real difference. I believe small towns win through partnerships – pooling our creative, technical, and financial
                    strengths.
                </p>
                <p>
                    I learned that early. At age 10 in Nichols, Iowa, I was a founding youth member of the Nichols Community Club – kids sat at the same table
                    as adults and voted. That experience cemented two ideas I carry today: everyone has gifts to share, and youth deserve a seat at the table.
                </p>
            </div>
        </div>
    </section>

    <!-- SIGN REQUEST (Netlify Forms) -->
    <section id="sign" class="section alt" aria-labelledby="sign-heading">
        <div class="wrap narrow">
            <h2 id="sign-heading" class="section-title">Request a Sign</h2>
            <form
                name="sign-request"
                method="POST"
                action="/"
                data-netlify="true"
                data-netlify-honeypot="bot-field"
                class="form"
                aria-describedby="privacy-note"
            >
                <input type="hidden" name="form-name" value="sign-request" />
                <p class="sr-only">
                    <label>Don’t fill this out if you’re human: <input name="bot-field" /></label>
                </p>

                <label class="sr-only" for="sr-name">Name</label>
                <input id="sr-name" name="name" placeholder="Name" required />

                <label class="sr-only" for="sr-address">Address</label>
                <textarea id="sr-address" name="address" rows="3" placeholder="Street, City, State, ZIP" required></textarea>

                <label class="sr-only" for="sr-phone">Phone</label>
                <input id="sr-phone" name="phone" placeholder="Phone" required />

                <label class="sr-only" for="sr-email">Email</label>
                <input id="sr-email" type="email" name="email" placeholder="Email" required />

                <button type="submit" class="btn primary">Send Request</button>
                <p id="privacy-note" class="small">We’ll verify before delivery. We’ll never share your info. Paid for by Ball for Bloomfield.</p>
            </form>
        </div>
    </section>

    <!-- CONTACT -->
    <section id="contact" class="section" aria-labelledby="contact-heading">
        <div class="wrap narrow">
            <h2 id="contact-heading" class="section-title">Contact</h2>
            <p>
                Email: <a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a>
                &middot; Facebook: <a href="https://facebook.com/BallForBloomfield">Ball for Bloomfield</a>
            </p>
        </div>
    </section>

    <!-- VISION NOTE -->
    <section class="section alt" aria-label="Vision">
        <div class="wrap narrow">
            <p>
                My vision is simple: fix what needs fixing, keep the basics strong, and create space for all of us - including our youth - to dream about what's
                next. Bloomfield works best when we work together.
            </p>
        </div>
    </section>
</main>

<footer class="footer" role="contentinfo">
    <div class="wrap small">Paid for by Ball for Bloomfield • © {new Date().getFullYear()}</div>
</footer>

<style>
    :root {
        /* Brand & base */
        --maroon: #660000;
        --ink: #0b1220;
        --muted: #5b6776;
        --bg: #ffffff;
        --surface: #f7f7f9;

        /* Fluid type & spacing */
        --fs-hero: clamp(1.9rem, 5.5vw, 3.2rem);
        --fs-h2: clamp(1.25rem, 2.2vw, 1.6rem);
        --fs-body: clamp(0.98rem, 1.2vw, 1.05rem);
        --radius: 12px;

        /* spacing scale */
        --s-1: 8px;
        --s-2: 12px;
        --s-3: 16px;
        --s-4: 20px;
        --s-5: 24px;
        --s-6: 28px;
        --s-7: 32px;
        --s-8: 40px;
    }

    html {
        font:
            16px/1.5 -apple-system,
            BlinkMacSystemFont,
            Segoe UI,
            Roboto,
            Helvetica,
            Arial,
            sans-serif;
    }
    body {
        margin: 0;
        background: var(--bg);
        color: var(--ink);
        font-size: var(--fs-body);
    }

    a {
        color: var(--maroon);
        text-decoration: none;
    }
    a:focus-visible,
    .btn:focus-visible,
    input:focus-visible,
    textarea:focus-visible {
        outline: 3px solid #1e90ff;
        outline-offset: 2px;
    }
    .sr-only {
        position: absolute;
        left: -10000px;
        top: auto;
        width: 1px;
        height: 1px;
        overflow: hidden;
    }

    .wrap {
        max-width: 1100px;
        margin: 0 auto;
        padding-inline: 18px;
    }
    .narrow {
        max-width: 760px;
    }

    /* HERO */
    .hero {
        background: var(--maroon);
        color: #fff;
        padding-block: clamp(32px, 7vw, 72px);
        border-bottom: 8px solid #5a0000;
    }
    .hero-logo {
        max-width: 320px;
        width: 100%;
        margin: 0 0 var(--s-3) 0;
        display: block;
    }
    .title {
        font-size: var(--fs-hero);
        margin: 0 0 var(--s-2);
        font-weight: 800;
    }
    .tag {
        margin: 0 0 var(--s-4);
        opacity: 0.95;
    }
    .cta {
        display: flex;
        gap: var(--s-2);
        flex-wrap: wrap;
    }
    .btn {
        display: inline-block;
        border: 2px solid #fff;
        border-radius: 10px;
        padding: 12px 16px;
        font-weight: 700;
        color: #fff;
    }
    .btn.primary {
        background: #fff;
        color: var(--maroon);
    }

    /* Sections */
    .section {
        padding-block: clamp(36px, 7vw, 56px);
    }
    .section.alt {
        background: var(--surface);
    }
    .section-title {
        margin: 0 0 var(--s-3);
        font-size: var(--fs-h2);
        font-weight: 700;
    }

    /* ABOUT */
    .about-grid {
        display: grid;
        gap: var(--s-3);
        align-items: start;
    }
    @media (min-width: 720px) {
        .about-grid {
            grid-template-columns: 180px 1fr;
        }
    }
    .headshot {
        width: 100%;
        max-width: 180px;
        height: auto;
        border-radius: var(--radius);
        display: block;
    }

    /* CARDS */
    .cards {
        display: grid;
        gap: var(--s-3);
        grid-template-columns: 1fr;
    }
    @media (min-width: 640px) {
        .cards {
            grid-template-columns: repeat(2, 1fr);
        }
    }
    @media (min-width: 960px) {
        .cards {
            grid-template-columns: repeat(3, 1fr);
        }
    }
    .card {
        background: #fff;
        border: 1px solid #e6e6ef;
        border-radius: var(--radius);
        padding: var(--s-3);
    }
    .card h3 {
        margin: 0 0 6px;
        font-size: 1.1rem;
    }
    .card p {
        margin: 0;
        color: #3c4658;
    }

    /* FORM */
    .form {
        display: grid;
        gap: 10px;
    }
    input,
    textarea {
        font: inherit;
        border: 1px solid #cfd7e6;
        border-radius: 10px;
        padding: 11px 12px;
        width: 100%;
        background: #fff;
    }
    button {
        cursor: pointer;
    }
    .small {
        color: var(--muted);
        font-size: 0.9rem;
    }

    /* FOOTER */
    .footer {
        background: #f3f5fa;
        color: #3b4656;
        padding-block: 28px;
    }

    /* Mobile tweaks */
    @media (max-width: 480px) {
        .cta .btn {
            width: 100%;
            text-align: center;
        }
        .section {
            padding-block: 40px;
        }
        .hero {
            padding-block: 40px;
        }
    }
    @media (prefers-reduced-motion: reduce) {
        * {
            scroll-behavior: auto;
        }
    }
</style>
